# PasswordCracker
Java program which simulates how password storage and authentication works by saving user accounts across three files: one in plain text, one hashed, and one salted and hashed. It also includes a brute-force cracker that systematically guesses every possible digit combination at a given length to recover passwords from the hashed and salted files.
 
